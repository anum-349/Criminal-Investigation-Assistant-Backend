version: "3.9"

services:
  fir-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      # Hot-reload: mount source so code changes reflect without rebuild
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped